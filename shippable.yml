language: java

jdk:
    - oraclejdk8

services:
    - rabbitmq
    - mongodb

before_script:
    - mkdir -p shippable/buildoutput

after_script:
    - cp ./target/schedule-*-SNAPSHOT.jar ./shippable/buildoutput/schedule.jar

